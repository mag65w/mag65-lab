# GitHub Personal Access Token
# Important: Must use Classic token (Fine-grained tokens may not work with GitHub Enterprise)
# To generate a token on GitHub:
# 1. GitHub Settings > Developer settings > Personal access tokens > Tokens (classic)
# 2. Click "Generate new token (classic)"
# 3. Select required permissions (at least public_repo permission needed, repo permission recommended for GitHub Enterprise)
# 4. Paste the generated token below
GITHUB_TOKEN=""

# GitHub Enterprise URL (optional)
# Only set this if using GitHub Enterprise.
# Example: https://github.enterprise.com or https://github.enterprise.com/api/v3
# If not set, default GitHub.com will be used.
GITHUB_ENTERPRISE_URL=""

